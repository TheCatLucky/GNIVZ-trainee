import React from 'react';

import styles from './About.module.scss';

const About: React.FC = () => (
  <div className={styles.component}>
      Представление постов в виде Списка или же Карточек.
  </div>
);

export default About;
